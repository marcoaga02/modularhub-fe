import { PickListItemDirective } from './pick-list-item.directive';

describe('PickListItemDirective', () => {
  it('should create an instance', () => {
    const directive = new PickListItemDirective();
    expect(directive).toBeTruthy();
  });
});
